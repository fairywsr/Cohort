//  You are organizing a party and have a list of invited guests. But just before the party starts, a
// new friend decides to join. You need to add them to the guest list.
// Problem Statement: Create a function that adds a new guest to the guest list and retums the
// updated list.

function CheckEvenOdd(num){
    if(num % 2 === 0){
        return 'Even';
    } else{
        return 'odd'
    }
}
console.log(CheckEvenOdd)